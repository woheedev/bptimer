{
  "name": "pb",
  "version": "0.0.1",
  "author": "Wohee",
  "description": "PocketBase backend for the BPSR Timer Panel monorepo",
  "license": "AGPL-3.0-or-later",
  "private": true,
  "scripts": {
    "build": "docker-compose build",
    "build:binary": "go build -o pocketbase .",
    "deploy": "fly deploy",
    "dev": "docker-compose up --build",
    "format": "go fmt ./...",
    "update": "go get -u ./... && go mod tidy"
  }
}
